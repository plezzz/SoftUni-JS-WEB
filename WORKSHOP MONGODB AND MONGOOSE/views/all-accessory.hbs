<main>
    {{#each accessory}}
        <div class="cube">
            <img class="cube" src="{{imageURL}}" alt="{{name}}">
            <p class="name">{{name}}</p>
            <a class="btn" href="/details-accessory/{{_id}}">Details</a>
        </div>
    {{/each}}
</main>
